#include<stdio.h>
void main()
{
    printf("hello world");  // here 'printf' is the standard Library function.This will prints text on screen.
}
/* --- standard library function --

>standard library function also called built-in function or predefined function(already defined inside a file).
>printf(),scanf() is the standard Library function.This will prints text on screen. using headerfile <stdio.h>
>getch() is the standard Library fuction. using headerfile <conio.h>
>sqrt(),pow(), is the standard Library fuction.This will prints square root. using headerfile <math.h>
>toupper(),tolower() is the standard Library fuction.This will prints lower letter to upper via. using headerfile <ctype.h>
>main() is also a function.
*/
