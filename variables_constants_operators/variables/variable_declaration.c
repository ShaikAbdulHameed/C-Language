#include<stdio.h>
void main()
{
    int age;    // a is a 'variable', int is 'integer datatype'.
    age=32;     //Assign to  variable 'age'.
    // if we run this code the variable is created but not get result for this we use printf() functioin.
    printf("c programming\n");
    printf("Age is %d",age);   //%d is the 'format specifier'.
}


/* ----variable--------
1) variable is used to change the value.
