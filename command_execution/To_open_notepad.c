#include<stdio.h>

int main()
{
  //system("notepad.exe");
          (or)
  system("C:\\Windows\\System32\\notepad.exe");

}

// to see .exe files path in our computer("C:\Windows\System32"). then search "notepad.exe"
//celsius_to_farhenheit ->compiler accepted
//celsius to farhenheit ->compiler not accepted spaces.
